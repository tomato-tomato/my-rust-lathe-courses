# Lathe 技能使用教程

Lathe 是一套面向 LLM 编码助手的**动手实践型教程生成技能体系**。通过一个统一的 `/lathe` 命令，你可以按需生成高质量的技术教程，并逐步续写、验证、提问和管理。

> 本教程适用于 **QoderWork** 或 **QoderWork CN** 桌面客户端。本仓库中的技能在这两款客户端中使用过；技能采用标准 Agent Skills 布局（SKILL.md + references/ 子目录），也已确认兼容 Reasonix 等支持该标准的终端 Agent（见[常见问题](#常见问题)），其他场景需要注意验证

## 目录

- [快速开始](#快速开始)
- [操作一览](#操作一览)
- [存储路径配置](#存储路径配置)
- [/lathe — 生成教程](#lathe--生成教程)
- [/lathe 续写 — 续写教程](#lathe-续写--续写教程)
- [/lathe 复习 — 复习教程](#lathe-复习--复习教程)
- [/lathe 验证 — 验证教程](#lathe-验证--验证教程)
- [/lathe 提问 — 提问教程内容](#lathe-提问--提问教程内容)
- [/lathe 标签 — 管理教程标签](#lathe-标签--管理教程标签)
- [/lathe 语气 — 创建自定义语气](#lathe-语气--创建自定义语气)
- [/lathe 列表 — 列出教程](#lathe-列表--列出教程)
- [完整工作流示例](#完整工作流示例)
- [教程文件结构参考](#教程文件结构参考)
- [常见问题](#常见问题)

---

## 快速开始

### 1. 下载技能包

从本仓库的 [GitHub Releases] 页面下载最新的 `skills` 压缩包并解压：

```bash
# 下载并解压（以实际 release 文件名为准，如 skills-0.5.0.zip）
unzip skills-0.5.0.zip -d ./skills-download
```

解压后的目录结构如下：

```
skills-download/
└── lathe/                          ← 统一技能文件夹
    ├── SKILL.md                     ← 入口路由 + 共享配置
    └── references/                  ← 操作模块（按需加载，请勿单独移动）
        ├── generate.md              ← 生成教程模块
        ├── extend.md                ← 续写教程模块
        ├── verify.md                ← 验证教程模块
        ├── ask.md                   ← 提问模块
        ├── tag.md                   ← 标签模块
        ├── voice.md                 ← 语气模块
        ├── list.md                  ← 列表模块
        ├── review.md                ← 复习模块
        └── style-benchmarks.md      ← 风格标杆（生成/续写时参考）
```

> 压缩包内仅包含 `lathe/` 技能文件夹本身；本使用教程不随包分发，可在本仓库的 `docs/skills-tutorial.md` 查阅。

> **提示：** 安装时必须连同 `references/` 子目录一起复制——它是技能的标准布局（Anthropic Agent Skills 规范），模块文件按需加载，能减少每次调用的上下文开销。

### 2. 安装技能

将解压后的技能文件夹复制到技能目录：

```bash
# 复制到用户级技能目录（所有项目可用，推荐）
cp -r skills-download/lathe ~/.qoderwork/skills/

# 或复制到项目级技能目录（仅当前项目可用）
mkdir -p .qoderwork/skills
cp -r skills-download/lathe .qoderwork/skills/
```

> **提示：** 用户级安装（`~/.qoderwork/skills/`）只需操作一次，之后在所有项目中都可使用这些技能。
>
> 本仓库中的技能已在 QoderWork 和 QoderWork CN 客户端上测试；如需在 Reasonix 中使用，将 `lathe/` 目录复制到 `~/.reasonix/skills/` 即可（见[常见问题](#常见问题)）。

### 3. 验证安装

在 QoderWork 或 QoderWork CN 客户端中输入 `/lathe`，如果技能正确加载，AI 会展示操作选项供你选择。

### 4. 生成第一个教程

```
/lathe 用 Go 构建一个 Raft 共识算法实现
```

AI 会引导你完成：确认保存位置与经验水平 → 锁定工具版本 → 研究主题 → 生成第一部分 → 自动存储。

---

## 操作一览

所有操作通过 `/lathe` 调用，后跟**意图关键词**（中文或英文均可）和参数：

| 操作         | 命令                      | 功能                                                | 写入文件                                                             |
| ------------ | ------------------------- | --------------------------------------------------- | -------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------- |
| **生成教程** | `/lathe <主题>`           | 生成教程的第一部分                                  | `part-01.md`、`metadata.json`、在 `~/.lathe/config.json` 登记位置    |
| **续写教程** | `/lathe 续写\\            | extend <slug> [指导]`                               | 为已有教程添加下一部分                                               | `part-NN.md`、更新 `metadata.json`                                                                              |
| **收尾完结** | `/lathe 续写 收尾 <slug>` | 生成总结部分并完结课程（`status` 置为 `completed`） | 总结 `part-NN.md`、更新 `metadata.json`                              |
| **复习教程** | `/lathe 复习\\            | review <slug>`                                      | 生成复习材料（不改变课程方向与状态）                                 | `part-NN-review.md`、更新 `metadata.json`                                                                       |
| **验证教程** | `/lathe 验证\\            | verify <slug>`                                      | 端到端验证教程是否可用，含版本冲突检测与修复；工作区持久化并增量复用 | `verify-result.json`、`.lathe-verify/` 工作区、同步 `metadata.json` 状态；版本冲突修复经确认后可改 `part-NN.md` |
| **提问内容** | `/lathe 提问\\            | ask <slug> [part] <问题>`                           | 回答关于教程某部分的问题（part 可省略，自动定位）                    | 无（只读）                                                                                                      |
| **管理标签** | `/lathe 标签\\            | tag <slug>`                                         | 管理教程的搜索标签                                                   | 更新 `metadata.json`                                                                                            |
| **创建语气** | `/lathe 语气\\            | voice [名称]`                                       | 创建自定义写作语气预设                                               | `voices/<名称>.md`                                                                                              |
| **列出教程** | `/lathe 列表\\            | list [slug]`                                        | 按语言分组列出所有教程，或查看某教程详情                             | 无（只读）                                                                                                      |

> **提示：** 也支持纯自然语言触发（无需 `/lathe`），AI 会自动识别意图。中文和英文关键词均可识别。当意图不明确时，AI 会展示选项菜单供你选择。

---

## 存储路径配置

教程存储位置记录在配置文件中，支持**多路径存储**——不同教程可以保存在不同目录：

**配置文件位置：** `~/.lathe/config.json`

```json
{
	"tutorials_base_path": "~/others/lathe_tutorials",
	"tutorial_paths": {
		"taskr": "~/others/lathe_tutorials",
		"raft-go": "~/projects/my-tutorials"
	}
}
```

| 字段                  | 含义                                                                             |
| --------------------- | -------------------------------------------------------------------------------- |
| `tutorials_base_path` | 默认基础目录：生成新教程时默认提议的位置；`voices/` 自定义语气目录也始终位于此处 |
| `tutorial_paths`      | 每个教程（slug）实际所在目录的映射，由技能自动维护                               |

### 首次使用 / 配置文件缺失

调用 `/lathe` 时若配置文件不存在，技能会询问你是第一次使用还是之前已经生成过教程：

- **第一次使用：** 询问是否使用默认路径 `~/others/lathe_tutorials/`（或指定新路径），确认后自动创建配置文件。
- **已经使用过：** 请提供已有教程的存放位置（可以是一个或多个目录）。技能会扫描这些位置、验证发现的教程，展示扫描结果经你确认后写入配置——这样分散在各处的教程都能被 `列表`/`续写`/`验证` 等命令找到。
- **已存在配置：** 静默读取，直接使用。

### 生成时的保存位置

每次生成新教程时，技能会询问本课程保存位置（默认提议 `tutorials_base_path`）。选择默认则与旧行为一致；指定新路径则该教程单独存到那里，并自动登记到 `tutorial_paths`。

### 修改存储配置

直接编辑配置文件即可：

```bash
# 查看当前配置
cat ~/.lathe/config.json

# 修改默认目录（新教程的默认提议位置）
echo '{"tutorials_base_path": "~/my-tutorials", "tutorial_paths": {}}' > ~/.lathe/config.json
```

> 修改 `tutorials_base_path` 后已有教程不会自动迁移，但 `tutorial_paths` 中已登记的教程仍可在原位置被找到——这正是多路径机制的优势。

### 操作已有教程时的位置解析

对已有教程执行操作（续写、验证、复习、提问、标签等）时，技能按以下顺序定位它的实际目录：

1. 先查 `tutorial_paths[<slug>]` 中登记的位置；
2. 未登记则回退到默认目录 `tutorials_base_path`；
3. 该位置也找不到此 slug 时，列出配置中已知的所有 slug 及其位置供你选择。

### 默认目录结构

```
~/others/lathe_tutorials/          ← 默认教程基础目录
├── <slug>/                        ← 每个教程一个子目录（也可位于其他已登记目录）
│   ├── metadata.json              ← 教程索引（顶部含防误删提醒，请勿删除；误删可自愈重建）
│   ├── part-01.md                 ← 第一部分
│   ├── part-02.md                 ← 第二部分（由 /lathe 续写 添加）
│   ├── verify-result.json         ← 验证结果（由 /lathe 验证 生成）
│   └── .lathe-verify/             ← 验证工作区（隐藏目录，首次验证后自动创建，可安全删除）
└── voices/                        ← 自定义语气预设（始终位于默认基础目录）
    └── <名称>.md                  ← voice 规范文件
```

> ⚠️ **请勿删除 `metadata.json`**——它是教程的索引文件。若误删或损坏，技能会在下次操作时自动检测到并从 `part-*.md` 正文重建（重建前会展示推断结果请你确认）。

---

## /lathe — 生成教程

### 功能

为任意编程主题生成动手实践型教程的第一部分。这是整个技能体系的核心入口。

### 调用方式

```
/lathe <主题描述>
```

**示例：**

```
/lathe 用 Zig 构建数字合成器
/lathe 用 Rust 实现一个键值存储
/lathe 用 Go 构建 Raft 共识算法
```

### 工作流程

调用后，AI 会按以下步骤引导你：

**1. 解析配置**
自动从 `~/.lathe/config.json` 读取配置。配置缺失时会询问你是第一次使用还是已生成过教程：第一次使用确认默认目录；已使用过则提供已有教程位置（可多个），技能扫描验证后写入配置（见[存储路径配置](#存储路径配置)）。

**2. 一轮分组提问**

AI 用**一条消息**集中收集写作前需要的全部信息，不再多轮往返：

- **保存位置（每次生成都问）**：默认提议配置中的默认目录，可指定其他路径
- **经验水平**：初学者、有一定了解、还是相关领域有经验？（同时用于后续概念讲解的锚点选择）
- **澄清问题**（仅在主题宽泛时出现，最多一个）
- **仓库**：是否针对特定代码仓库（URL 和分支）
- **工具链版本与学习目标**：如 _"我将基于 **Go 1.22** 来写——可以吗？"_；同时 AI 会基于主题与你的回答提炼一句可评估的**学习目标**（`goal`）一并请你确认——它是后续续写时"目标达成回顾"与收尾完结的判定基准

对某项未作答时，AI 会采用合理默认值并告知，不再追问。

**3. 确定教程语言**
教程正文语言默认跟随你的对话语言，也可显式指定（如"用英文写"），结果记录在 `metadata.json` 的 `language` 字段，后续续写/提问自动沿用。

**4. 研究主题**
AI 会通过网络搜索查阅权威来源（官方文档、论文、源代码等），确保教程内容基于真实资料而非模型记忆。研究采用**摘要优先**策略：先从搜索结果标题与摘要中提取事实，仅对关键来源做全文抓取，查阅来源总数不超过 5 个——在保证准确性的同时控制耗时与开销。同时执行**版本-特性一致性检查**：示例代码使用的特性必须在锁定版本下成立（如 Rust edition 与代码语义匹配），不确定时查官方 changelog。

> 如果当前会话没有网络工具，AI 会提前告知，并在关键声明处标记 `[!UNVERIFIED]`。

**5. 生成第一部分**
AI 在内部完成预检（选择控制性示例、确定章节结构、校验版本-特性一致性等）后，撰写 `part-01.md`。

**6. 存储教程**
将 `part-01.md` 和 `metadata.json` 写入你确认的保存位置，并在 `~/.lathe/config.json` 的 `tutorial_paths` 中登记该教程位置，然后告知你：

> **教程已保存**到 `~/others/lathe_tutorials/raft-go/`。
> 这是第一部分。要添加更多部分，调用 `/lathe 续写 raft-go`。
> 验证是可选的：调用 `/lathe 验证 raft-go`。
> 要对某个部分提问，调用 `/lathe 提问 raft-go part-01.md`。

### 教程质量标准

每篇教程遵循以下标准：

- **控制性示例：** 贯穿始终的具体产物（如"一个 4 声部减法合成器"），不使用 `foo`/`bar`
- **概念锚定：** 重要概念除详细介绍外，还会从你已知的知识体系引申讲解（锚点随你回答的经验水平调整），帮助更快理解
- **具体数字：** 采样率、缓冲区大小、页面大小等，避免模糊的"很快"/"很大"
- **内联引用：** 关键事实旁直接附上来源链接
- **检查点：** 每个部分末尾有可执行的验证命令和预期输出
- **标注系统：** 使用 `> [!PREDICT]`、`> [!RECALL]`、`> [!HEADS-UP]` 等标注增强学习效果
- **风格标杆：** 根据主题场景匹配一位主锚点标杆作者（如 Raft 类分布式系统 → Sam Who、编译器 → Robert Nystrom、Rust 语言 → The Rust Book 作者），侧重其长项指导写作节奏与章节结构；完整标杆体系见技能包内的 `references/style-benchmarks.md`

---

## /lathe 续写 — 续写教程

### 功能

为已有教程添加下一部分。新部分会继承教程的控制性示例、数字、语气和工具版本。

### 调用方式

```
/lathe 续写 <slug> [指导...]
# 或使用英文关键词
/lathe extend <slug> [指导...]
```

**示例：**

```
/lathe 续写 raft-go
/lathe extend raft-go
/lathe 续写 raft-go 这一部分实现日志复制
/lathe extend raft-go 这一部分实现日志复制
/lathe 续写 digital-synth-zig 添加低通滤波器
```

如果不提供指导，AI 会根据上一部分末尾的"接下来"方向自动延续。

### 工作流程

1. **吸收现有教程** — 阅读所有已有部分和 `metadata.json`，理解控制性示例、固定数字、voice 等上下文；同时对照 `goal`（学习目标）评估达成度

2. **询问本地项目路径（可选）** — 如果你的教程涉及实战项目（如命令行工具、Web 应用等），AI 会提示你是否提供本地项目路径：

- **提供路径** — AI 会读取你当前的代码状态，基于实际进度续写下一部分，内容更贴合你的实际开发情况
- **跳过** — AI 基于教程中描述的代码状态续写

> 路径会记录到 `metadata.json` 的 `local_project_path` 字段，后续续写时可直接复用。

3. **研究新内容** — 与 `/lathe` 相同的研究纪律：查阅权威来源，记录 URL

4. **确定部分编号** — 自动递增（如已有 `part-01.md`，下一个是 `part-02.md`；已有复习材料时在其最大编号后递增）

5. **写入新部分** — 遵循完整的教程结构，以 `> [!RECALL]` 间隔检索开头；并在 `## 接下来` 前插入 **`## 目标达成回顾`** 小节：陈述原定学习目标、已覆盖/未覆盖内容、达成结论——由你自行判断继续延伸还是收尾完结

6. **更新 metadata.json** — 追加新部分到 `parts` 数组，追加新来源，`status` 重置为 `unverified`（若课程已完结，此操作自动撤销完结、重新开放）

7. **报告完成** — 告知你如何查看和验证

### 目标达成与收尾完结

生成教程时会确认一个**学习目标**（`metadata.json` 的 `goal` 字段；旧教程首次续写时自动推断补上）。每次续写都会对照目标评估达成度并写入新部分，你可以随时选择收尾：

```
/lathe 续写 收尾 <slug>
```

收尾模式生成一个**总结部分**：全课程逐部分回顾、知识图谱（Mermaid）、目标对照、练习方向、寄语，以及一段回顾自检问题。完成后 `status` 标记为 `completed`，`/lathe 列表` 中显示 ⭐ 已完结。

> 完结不是永久的：之后直接 `/lathe 续写 <slug>` 即可重新开放课程（`status` 自动重置）；`/lathe 复习 <slug>` 则不会改变完结状态。

### 注意事项

- 每次只写一个部分，不批量生成
- 不修改已有的部分文件
- 继承相同的工具版本，不静默升级
- 如果读者想迁移到新版本，建议创建新教程而非在原教程中漂移

---

## /lathe 复习 — 复习教程

### 功能

为久未学习、想先复习再精进的读者生成结构化复习材料。复习不引入新内容、不改变课程方向，也不影响课程状态（已完结的课程复习后仍保持完结）。

### 调用方式

```
/lathe 复习 <slug>
# 或使用英文关键词
/lathe review <slug>
```

### 复习材料

生成文件命名为**最新部分编号 + 1 并附 `-review` 后缀**（如已有 part-01~03，则生成 `part-04-review.md`），包含：

- **逐部分回顾** — 每部分构建了什么、核心概念、关键数字；
- **自测：间隔检索** — 5-8 个不给答案的问题，覆盖各部分关键概念；
- **重跑检查点** — 摘录各部分检查点命令与预期输出，供本地重执行；
- **学习目标回顾** — 对照 `goal` 说明已建立的能力；
- **还可以继续深入的方向** — 3-5 个延伸方向，想继续时直接 `/lathe 续写 <slug>`。

复习材料计入 `parts` 但不参与验证（`/lathe 验证` 会跳过 `-review` 文件），`/lathe 列表` 中标注 _（复习）_。

---

## /lathe 验证 — 验证教程

### 功能

在教程目录下的验证工作区（`.lathe-verify/`）中，像真实读者一样逐步执行教程，检查每步是否确实可用。工作区持久保留并增量复用——再次验证时不重新生成已有代码。

### 调用方式

```
/lathe 验证 <slug>
# 或使用英文关键词
/lathe verify <slug>
```

**示例：**

```
/lathe 验证 raft-go
/lathe verify raft-go
```

### 工作流程

1. **读取元数据** — 获取部分列表和所需工具
2. **版本一致性静态预检** — 不运行代码，先比对三类版本信息：`metadata.json` 锁定的工具链版本、配置代码块声明的版本（如 `Cargo.toml` 的 `edition`）、示例代码实际使用特性所需的最低版本。发现冲突（如 edition 2024 配 2021 语义的代码）直接进入修复流程，不等到运行才暴露
3. **标记为进行中** — 写入 `verify-result.json`，状态设为 `verifying`
4. **准备验证工作区** — 位于教程目录下的 `.lathe-verify/`（首次验证时创建并在 `metadata.json` 中记录说明）；工作区已存在时**增量复用**：上次已通过且对应部分未修改的直接跳过，从第一个需要执行的部分（失败/未跑/内容已变更/新增）继续，依赖与编译缓存直接复用；针对仓库的教程（`repo` 非空）在工作区内克隆对应分支
5. **逐步执行** — 按确定范围安装前置条件、创建文件、粘贴代码、运行检查点命令；报错时先归因——版本冲突类错误进入修复流程，其他错误按失败处理
6. **记录结果** — 更新 `verify-result.json`（逐部分明细），并同步 `metadata.json` 的 `status`；工作区保留供下次增量验证使用

### 版本冲突检测与修复

这是验证环节重点强化的能力，专门解决"教程示例声明的版本与文本介绍的特性相冲突，读者照着敲代码才发现报错"这类问题：

1. **展示冲突详情**：所在部分、代码块位置、冲突的版本与特性，附证据（编译错误或官方文档链接）
2. **提出两个修复方向**并给出建议：

- **方案 A**：改代码/文本对齐锁定版本（通常首选）
- **方案 B**：调整版本声明并同步更新 `metadata.json` 的 `tools`

3. **你确认后才修改**，改动最小化（只动冲突相关内容，不改 voice 和结构）；拒绝修复则保留原文按失败记录
4. **修复后重新执行该部分**，确认真正生效；修复内容记入 `verify-result.json` 对应部分的 `fixes` 数组

### 安全护栏

- 所有命令只在验证工作区（`.lathe-verify/`）内执行，不碰你的项目和全局环境
- 需要 `sudo`、全局安装、工作区外删除等敏感操作时会先向你确认，未确认则记为 `skipped`
- 长时间运行的命令后台执行并验证后终止，单命令约 5 分钟无进展视为异常
- 验证结束后工作区**静默保留**（不会询问是否清理）；它是隐藏目录，可以安全手动删除——下次验证会自动重建并全量执行

### 验证结果

验证完成后会产生三种状态之一，并附逐部分明细（`passed` / `failed` / `not_run`）：

| 状态       | 含义     | 场景                                                                       |
| ---------- | -------- | -------------------------------------------------------------------------- |
| `verified` | 验证通过 | 所有检查点输出与教程声明一致（含经版本冲突修复后通过，修复记录在 `fixes`） |
| `skipped`  | 跳过     | 缺少必需工具，或敏感操作未获确认                                           |
| `failed`   | 验证失败 | 代码无法编译、输出不匹配、步骤矛盾（非版本冲突类，或你拒绝了修复）         |

验证通过后 `metadata.json` 的 `status` 会同步更新为 `verified`。

### 注意事项

- 验证对教程部分**默认只读**；唯一例外是版本冲突修复，且必须先获你确认、改动最小化
- `skipped` 不等于 `failed`——缺少工具是环境问题，不是教程问题
- 验证前需要本地安装教程所需的工具链

---

## /lathe 提问 — 提问教程内容

### 功能

针对教程的特定部分提出问题，AI 会基于该教程的具体内容回答，而非泛泛地重新讲解主题。

### 调用方式

```
/lathe 提问 <slug> [part] <你的问题>

# 或使用英文关键词
/lathe ask <slug> [part] <your question>
```

**part 参数宽松匹配：** `part-01.md`、`part-01`、`1` 均可；也可以**直接省略**，AI 会根据问题内容自动定位最相关的部分，无法判断时列出部分列表让你选择。

**示例：**

```
/lathe 提问 raft-go part-01
为什么用环形缓冲区而不是 channel？

/lathe ask raft-go 1
为什么用环形缓冲区而不是 channel？

/lathe 提问 digital-synth-zig
如果滤波器截止频率超过奈奎斯特频率会怎样？（省略 part，自动定位）
```

### 回答原则

- **扎根于本教程** — 使用教程中相同的示例、数字和术语回答
- **指向具体位置** — 优先"看 §3 中的 `process_buffer` 循环"而非从头推导
- **对缺口诚实** — 如果问题暴露了教程的错误或含糊之处，会直接说明
- **对话式** — 保持会话以回答后续追问

---

## /lathe 标签 — 管理教程标签

### 功能

为教程选择或更新搜索标签，使教程更容易被发现和分类。

### 调用方式

```
/lathe 标签 <slug>
# 或使用英文关键词
/lathe tag <slug>
```

**示例：**

```
/lathe 标签 raft-go
/lathe tag raft-go
```

### 标签规范

AI 会选择 2-5 个小写、可复用的标签，涵盖以下维度：

| 维度            | 示例                                            |
| --------------- | ----------------------------------------------- |
| **语言/运行时** | `go`、`rust`、`zig`、`python`                   |
| **领域**        | `compilers`、`databases`、`audio`、`networking` |
| **核心技术**    | `consensus`、`dsp`、`parsing`、`concurrency`    |

优先选择能与其他教程自然分组的通用标签，避免过于具体的一次性标签。

### 操作方式

- **设置标签：** 完全替换 `tags` 数组
- **添加标签：** 追加到现有数组并去重
- **移除标签：** 从现有数组中删除

标签保存在 `metadata.json` 的 `tags` 字段中。

---

## /lathe 语气 — 创建自定义语气

### 功能

创建自定义的写作语气（Voice）预设，控制教程的文风、视角和表达方式。

### 内置 Voice

| Voice                   | 特点                                                                       | 适用场景                   |
| ----------------------- | -------------------------------------------------------------------------- | -------------------------- |
| **plainspoken**（默认） | 直接、精确、不虚构人格。平实散文，信任读者                                 | 大多数技术教程             |
| **companion**           | 温暖、幽默、第一人称。键盘旁的老朋友                                       | 入门教程、需要亲和力的场景 |
| **craftsman**           | 从容的匠人师傅。散文式构建，长短句交替，类比贯穿，先讲"为什么"再讲"怎么做" | 需要深度讲解设计决策的教程 |

### 调用方式

```
/lathe 语气 [名称]
# 或使用英文关键词
/lathe voice [name]
```

**示例：**

```
/lathe 语气 terse
/lathe voice terse
/lathe 语气 field-notes
/lathe voice field-notes
```

### 创建流程

1. **选择名称** — 提供或确认一个简短的 slug（如 `terse`、`socratic`）
2. **风格访谈** — AI 会询问：

- 正式 ↔ 随意？密集 ↔ 疏朗？冷峻 ↔ 温暖？
- 人称：第一人称、协作式还是非人称？
- 幽默程度：无、克制还是活泼？
- 这种 voice 绝不应该像什么？

3. **起草规范** — 生成包含三个章节的 markdown 文件：风格与调性、避免、校准对比
4. **展示并确认** — 展示完整草稿，迭代直到你批准
5. **持久化** — 写入 `<TUTORIALS_DIR>/voices/<名称>.md`

### 使用自定义 Voice

创建后，在生成教程时指定 voice：

```
/lathe 用 Rust 构建解析器，使用 terse voice
```

Voice 名称会记录在 `metadata.json` 中，`/lathe 续写` 会自动继承。

### 管理 Voice

- **查看已有 voice：** 检查 `<TUTORIALS_DIR>/voices/` 目录
- **删除 voice：** 直接删除对应的 `.md` 文件
- **内置 voice 不可覆盖：** `plainspoken`、`companion` 和 `craftsman` 保留为内置

### 安全护栏

技能会拒绝创建以下 voice：

- 冒充真实人物的 voice
- 虚构资历或权威背书的 voice
- 试图将 AI 生成内容伪装为人类撰写的 voice

---

## /lathe 列表 — 列出教程

### 功能

列出所有已存储的教程，或查看单个教程的详细状态。忘记 slug、想了解整体学习进度时非常有用。完全只读，不修改任何文件。

### 调用方式

```
/lathe 列表          # 列出所有教程
/lathe list          # 同上（英文关键词）
/lathe 列表 raft-go  # 查看单个教程详情
/lathe list raft-go
```

### 展示方式

**按编程语言分组：** 如果你同时学习多门语言（如 Rust 和 Zig），教程会按语言分大类展示；只有一门语言时不分组。语言从 `tags` 中的语言标签识别，缺失时从工具链/主题推断。

**多路径枚举：** 列表会扫描配置中登记的所有目录（`tutorial_paths` 各值 + 默认目录），保存在不同位置的教程都能被找到；非默认目录下的教程会额外标注所在路径。若发现某目录有 `part-*.md` 但缺 `metadata.json`，会单独标注 _⚠️ metadata 缺失_ 并提示可自愈重建。

**课程逐部分展示：** 每门课程展示全部部分及各部分标题，并标注验证结果：

```
### Rust

**todo-cli-rust** — 用 Rust 构建命令行待办事项应用
状态：verified ｜ 最近验证：verified 2026-07-20 ｜ 标签：rust, cli, todo ｜ 正文语言：zh-CN
部分（3）：
1. part-01.md — 搭建项目骨架与基础命令 ✅ passed
2. part-02.md — 添加优先级与标签 ✅ passed
3. part-03.md — 模块化重构与集成测试 ✅ passed

### Zig

**digital-synth-zig** — 用 Zig 构建数字合成器
状态：unverified ｜ 最近验证：— ｜ 标签：zig, audio, dsp ｜ 正文语言：zh-CN
部分（1）：
1. part-01.md — 生成第一个正弦波

共 2 门语言、2 门课程、4 个部分，1 门课程尚未验证。
```

**单教程详情：** 展示标题、主题、编程语言、正文语言、voice、部分列表（含逐部分验证结果）、锁定工具版本、验证结果等，并根据状态给出下一步建议（如未验证 → 提示可验证）。

---

## 完整工作流示例

以下是一个从零开始、完整使用所有操作的示例流程：

### 第一步：创建配置（首次使用）

```
用户：/lathe 用 Go 构建 Raft 共识算法

AI：检测到还没有配置文件。这是你第一次使用 Lathe，还是之前已经生成过教程？

用户：第一次使用

AI：教程默认存储在 ~/others/lathe_tutorials/，要使用其他位置吗？

用户：默认就好
```

> 如果你之前已生成过教程（可能分散在多个目录），回答"已经使用过"并提供这些位置，技能会扫描验证后写入配置，让所有已有教程重新可见。

### 第二步：一轮分组提问并生成

```
AI：已创建配置。开始前请一次性确认几件事：
    1. 本教程将保存到 ~/others/lathe_tutorials/（默认目录），要换个位置吗？
    2. 你在分布式系统领域的经验水平——初学者、有一定了解、还是相关领域有经验？
    3. 这个教程是独立教程，还是针对特定仓库？
    4. 我将基于 Go 1.22 来写；学习目标定为"掌握 Raft 核心机制（Leader 选举、
       日志复制、安全性）并用 Go 从零实现"——可以吗？

用户：默认位置，有一定了解，独立教程，Go 1.22 和学习目标都可以

AI：（研究主题中...以摘要优先策略查阅了 Raft 论文、Go 标准库文档等来源，
      并校验示例代码特性与 Go 1.22 兼容）
    （生成 part-01.md 和 metadata.json，正文语言跟随对话记为 zh-CN，
      并在配置的 tutorial_paths 中登记 raft-go 的位置）

    教程已保存到 ~/others/lathe_tutorials/raft-go/。
    要添加更多部分，调用 /lathe 续写 raft-go。
```

### 第三步：续写更多部分

```
用户：/lathe 续写 raft-go 这一部分实现 Leader 选举
# 或者：/lathe extend raft-go 这一部分实现 Leader 选举

AI：（阅读已有教程）
    你是否有已完成的本地项目代码？如果有，提供项目路径可以让我基于你当前的实际代码来续写下一部分课程。

    1. ✅ 提供项目路径 — 我会读取项目代码，基于当前进度续写
    2. ⏭️ 跳过 — 基于教程中描述的状态续写

    请回复编号，或直接输入项目路径。

用户：2

AI：（研究 Leader 选举，生成 part-02.md）
    已添加 part-02.md。可以调用 /lathe 验证 raft-go 验证。
```

### 第四步：验证教程

```
用户：/lathe 验证 raft-go
# 或者：/lathe verify raft-go

AI：（版本一致性预检：比对锁定版本、配置声明与代码特性，未发现冲突）
    （在验证工作区 .lathe-verify/ 中逐步执行，运行检查点命令）
    验证结果：
    - part-01.md：✓ 通过
    - part-02.md：✓ 通过
    教程验证全部通过！metadata.json 状态已同步为 verified。
```

> 若预检或执行中发现版本冲突（如示例声明的 edition 与代码语义不匹配），AI 会展示冲突详情和两个修复方案，征得你同意后修复并重新验证。

### 第五步：提问和打标签

```
用户：/lathe 提问 raft-go part-02
     为什么选举超时要随机化？
# 或者省略 part：/lathe ask raft-go 为什么选举超时要随机化？

AI：在 part-02.md 的 "选举超时" 章节中，随机化是为了防止
    两个候选者同时发起选举导致的活锁...

用户：/lathe 标签 raft-go
# 或者：/lathe tag raft-go

AI：已为教程设置标签：["go", "distributed-systems", "consensus", "raft"]
```

### 第六步（可选）：收尾完结与复习

```
用户：/lathe 续写 收尾 raft-go

AI：（阅读全部部分并对照学习目标评估达成度）
    （生成总结部分：全课程回顾、知识图谱、目标对照、练习方向、寄语）
    课程已完结，status 已标记为 completed。
    想继续精进时直接 /lathe 续写 raft-go 即可重新开放。

# 之后若久未学习想先找回状态（不会改变完结状态）：
用户：/lathe 复习 raft-go

AI：已生成 part-NN-review.md：逐部分回顾、自测问题、
    可重跑的检查点命令，以及继续深入的方向。
```

### 第七步（可选）：创建自定义语气

```
用户：/lathe 语气 socratic
# 或者：/lathe voice socratic

AI：（进行风格访谈，起草 voice 规范）
    这是 socratic voice 的草稿...是否满意？

用户：很好

AI：已保存到 ~/others/lathe_tutorials/voices/socratic.md。
    下次生成教程时可以指定：/lathe <主题>，使用 socratic voice
```

---

## 教程文件结构参考

### metadata.json

每个教程的索引文件，记录教程的完整信息。**请勿删除**——顶部含防误删提醒，误删后技能可从 `part-*.md` 正文自愈重建：

```json
{
	"_warning": "⚠️ 本文件是该教程的索引文件，请勿删除。误删后 lathe 将无法识别此教程；可运行 /lathe 列表 触发自愈重建。",
	"slug": "raft-go",
	"title": "用 Go 从零实现 Raft 共识算法",
	"topic": "用 Go 构建 Raft 共识算法实现",
	"created": "2025-01-15T10:00:00Z",
	"status": "unverified",
	"tags": ["go", "distributed-systems", "consensus", "raft"],
	"parts": ["part-01.md", "part-02.md"],
	"language": "zh-CN",
	"repo": "",
	"repo_branch": "",
	"local_project_path": "",
	"tools": [{ "name": "Go", "version": "1.22" }],
	"sources": ["https://raft.github.io/raft.pdf", "https://pkg.go.dev/sync"],
	"voice": "plainspoken",
	"model": "QoderWork"
}
```

**字段说明：**

| 字段                 | 说明                                                                                                                    |
| -------------------- | ----------------------------------------------------------------------------------------------------------------------- |
| `_warning`           | 防误删提醒（技能读取时忽略；旧教程下次写回时自动补上）                                                                  |
| `slug`               | 教程的唯一标识符（URL 友好的短名称）                                                                                    |
| `title`              | 教程标题（来自 part-01.md 的 H1）                                                                                       |
| `topic`              | 用户的原始主题描述                                                                                                      |
| `goal`               | 学习目标（生成时确认；续写时目标达成评估与收尾完结的判定基准；旧教程首次续写时自动推断补上）                            |
| `created`            | 创建时间（ISO 8601）                                                                                                    |
| `status`             | 状态：`unverified` / `verified` / `completed`（已完结；续写会自动撤销完结）                                             |
| `tags`               | 标签数组（由 `/lathe 标签` 管理）                                                                                       |
| `parts`              | 已有的部分文件列表（含 `part-NN-review.md` 复习材料）                                                                   |
| `language`           | 教程正文语言（如 `zh-CN`、`en`），续写/提问自动沿用                                                                     |
| `repo`               | 关联的代码仓库 URL（独立教程为空）                                                                                      |
| `local_project_path` | 本地实战项目路径（由续写操作填写）                                                                                      |
| `tools`              | 锁定的工具链及版本                                                                                                      |
| `sources`            | 研究期间查阅的来源 URL（去重）                                                                                          |
| `voice`              | 使用的写作语气                                                                                                          |
| `model`              | 生成最后部分的 AI 模型                                                                                                  |
| `verify_workspace`   | 验证工作区说明（首次验证时写入）：指向教程目录下的 `.lathe-verify/`，并说明其作用（验证执行环境、增量复用、可安全删除） |

### 部分文件（part-NN.md）

每个部分遵循统一结构：

````markdown
# [标题]

[引子段落]

## 你将构建什么

[控制性示例的具体描述]

## 前置条件

[需要安装的工具和前置知识]

## [具体章节标题]

[教学内容，含代码块、旁白、设计笔记]

## 检查点

> [!PREDICT]
> 运行之前：你预期会看到什么输出？

**运行以下命令验证：**

```bash
<命令>
```

预期输出：...
**可能的错误：** ...

## 接下来

[未决问题和前进方向]

## 练习

1. [具体练习]
2. [具体练习]

## 来源

1. [标题](url) —— 说明
````

### verify-result.json

验证结果文件，记录整体状态和逐部分明细；经版本冲突修复后通过的部分会附 `fixes` 记录：

```json
{
	"status": "verified",
	"checked_at": "2025-01-15T10:30:00Z",
	"parts": [
		{ "part": "part-01.md", "status": "passed" },
		{
			"part": "part-02.md",
			"status": "passed",
			"fixes": [
				"Cargo.toml 锁定 edition 2024，但测试代码按 2021 语义编写；已改写为 edition 2024 写法"
			]
		}
	]
}
```

失败时 `parts` 中对应部分会包含 `failed_step`（部分内代码块与检查点命令的顺序编号）和 `error` 字段，未执行的部分记为 `not_run`。

### 标注类型速查

| 标注               | 用途                      | 使用频率          |
| ------------------ | ------------------------- | ----------------- |
| `> [!PREDICT]`     | 检查点前的预测提示        | 每个部分 1 次     |
| `> [!RECALL]`      | 第 N≥2 部分顶部的间隔检索 | 每个部分 1 次     |
| `> [!HEADS-UP]`    | 即将踩坑的警告            | 按需，每部分≤2 次 |
| `> [!ASIDE]`       | 词源、故事等旁白          | 按需              |
| `> [!DESIGN-NOTE]` | 多段落的"为什么"讨论      | 放在章节末尾      |
| `> [!UNVERIFIED]`  | 无法确认的关键声明        | 诚实标记          |
| `> [!NOTE]`        | 中性补充信息              | 按需              |
| `> [!TIP]`         | 便捷技巧                  | 按需              |

> 非教学性标注（HEADS-UP、ASIDE、NOTE、TIP）每部分合计不超过 3 个。

---

## 常见问题

### Q：教程可以跨多个会话续写吗？

可以。每次调用 `/lathe 续写`（或 `/lathe extend`）时，AI 会从 `metadata.json` 和已有部分文件中读取完整上下文，因此即使换了会话甚至换了 AI 模型，也能无缝续写。

### Q：可以在不同的 AI 工具之间切换使用吗？

技能采用标准的 Agent Skills 布局（`SKILL.md` 入口 + `references/` 子目录），这是 Anthropic 提出的开放规范，多个 AI 工具均已支持：

| 客户端                              | 用户级目录             | 项目级目录                               | 状态                                                                                      |
| ----------------------------------- | ---------------------- | ---------------------------------------- | ----------------------------------------------------------------------------------------- |
| QoderWork / QoderWork CN            | `~/.qoderwork/skills/` | `.qoderwork/skills/`                     | ✅ 已测试                                                                                 |
| Reasonix（DeepSeek 原生终端 Agent） | `~/.reasonix/skills/`  | `.reasonix/skills/` 或 `.claude/skills/` | ✅ 机制已验证（原生支持 SKILL.md 标准，自动注入 references/ 内容，配合 DeepSeek V4 使用） |

在未列出的其他工具（如 Qoder、Qwen Code 等）中可能无法正常工作，使用前建议先小规模验证。

> **提示：** 教程数据由 `~/.lathe/config.json` 管理（默认目录 `~/others/lathe_tutorials/`，也支持多路径存储），与具体客户端无关——在 QoderWork 和 Reasonix 之间切换时，教程、标签、验证结果完全共享。

### Q：验证失败了怎么办？

查看 `verify-result.json` 中 `parts` 数组里失败部分的 `failed_step` 和 `error` 字段定位问题。常见原因：

- 代码块中的代码不完整或有遗漏
- 检查点命令的预期输出与实际不符
- 前置条件遗漏了某个工具

注意：**版本冲突类问题（如示例声明的版本与代码使用的特性不匹配）不会直接记为失败**——验证时会自动检测并在你确认后修复，修复记录在 `fixes` 字段。其他问题修复教程后重新运行 `/lathe 验证`（或 `/lathe verify`）。

### Q：如何修改已生成教程的内容？

直接在编辑器中修改 `part-NN.md` 文件即可。修改后建议重新运行 `/lathe 验证`（或 `/lathe verify`）确认可用性。

### Q：Voice 会影响教程的准确性吗？

不会。Voice 仅控制语气和风格（如是否使用第一人称、幽默程度等）。教程的准确性、研究纪律、引用规范和验证规则是不变量，不受 Voice 影响。

### Q：如何查看所有已生成的教程？

直接使用 `/lathe 列表`（或 `/lathe list`），教程会按编程语言分组展示，每门课程逐部分列出并附验证状态。也可以直接浏览教程基础目录（默认为 `~/others/lathe_tutorials/`），或通过标签筛选：

```bash
# 查找包含特定标签的教程
grep -rl '"compilers"' ~/others/lathe_tutorials/*/metadata.json
```

### Q：教程分散在多个目录，还能统一管理吗？

可以。若配置文件丢失，调用 `/lathe` 时回答"已经使用过"并提供所有教程位置，技能会扫描验证并逐个登记到 `tutorial_paths`；之后生成新教程时也可随时指定不同的保存位置。`/lathe 列表` 会扫描所有已登记目录，非默认目录下的教程会标注所在路径。

### Q：误删了某个教程的 metadata.json 怎么办？

运行 `/lathe 列表` 会看到该教程被标注为 _⚠️ metadata 缺失_。随后对它执行任一写入型操作（如 `/lathe 标签 <slug>` 或 `/lathe 续写 <slug>`），技能会自动检测到缺失，从 `part-*.md` 正文推断各字段（标题、语言、工具版本、来源等），展示推断结果经你确认后重建索引——教程正文本身不会被动到。

### Q：意图不明确时会发生什么？

当你只输入 `/lathe` 或输入含糊的内容时，AI 不会默认生成教程，而是展示一个操作选项菜单（生成 / 续写 extend / 验证 verify / 提问 ask / 标签 tag / 语气 voice / 列表 list），让你选择想要执行的操作（复习 review 通过关键词显式触发）。
